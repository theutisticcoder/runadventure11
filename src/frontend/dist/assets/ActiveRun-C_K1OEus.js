import { c as createLucideIcon, r as reactExports, j as jsxRuntimeExports, S as Swords, d as Button, B as BookOpen, e as Skeleton, b as useNavigate, f as useSearch, M as MapPin } from "./index-DMIBnIiZ.js";
import { b as GENRE_BG_COLORS, a as GENRE_COLORS, B as Badge, G as GENRE_BUTTON_COLORS, c as GENRE_GLOW } from "./types-HpiJbp6e.js";
import { u as useBackend } from "./useBackend-CZ7kvxeE.js";
import { P as Play } from "./play-DRMtjgvO.js";
import { L } from "./leaflet-src-Ceggw5tJ.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["path", { d: "M15 3h6v6", key: "1q9fwt" }],
  ["path", { d: "M10 14 21 3", key: "gplh6r" }],
  ["path", { d: "M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6", key: "a6xqqp" }]
];
const ExternalLink = createLucideIcon("external-link", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["rect", { x: "14", y: "4", width: "4", height: "16", rx: "1", key: "zuxfzm" }],
  ["rect", { x: "6", y: "4", width: "4", height: "16", rx: "1", key: "1okwgv" }]
];
const Pause = createLucideIcon("pause", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  [
    "path",
    {
      d: "M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z",
      key: "1c8476"
    }
  ],
  ["path", { d: "M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7", key: "1ydtos" }],
  ["path", { d: "M7 3v4a1 1 0 0 0 1 1h7", key: "t51u73" }]
];
const Save = createLucideIcon("save", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["polygon", { points: "5 4 15 12 5 20 5 4", key: "16p6eg" }],
  ["line", { x1: "19", x2: "19", y1: "5", y2: "19", key: "futhcm" }]
];
const SkipForward = createLucideIcon("skip-forward", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", key: "afitv7" }]
];
const Square = createLucideIcon("square", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["path", { d: "M3 6h18", key: "d0wm0j" }],
  ["path", { d: "M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6", key: "4alrt4" }],
  ["path", { d: "M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2", key: "v07s0e" }],
  ["line", { x1: "10", x2: "10", y1: "11", y2: "17", key: "1uufr5" }],
  ["line", { x1: "14", x2: "14", y1: "11", y2: "17", key: "xtxkd" }]
];
const Trash2 = createLucideIcon("trash-2", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  [
    "path",
    {
      d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
      key: "wmoenq"
    }
  ],
  ["path", { d: "M12 9v4", key: "juzpu7" }],
  ["path", { d: "M12 17h.01", key: "p32p05" }]
];
const TriangleAlert = createLucideIcon("triangle-alert", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z",
      key: "uqj9uw"
    }
  ],
  ["path", { d: "M16 9a5 5 0 0 1 0 6", key: "1q6k2b" }],
  ["path", { d: "M19.364 18.364a9 9 0 0 0 0-12.728", key: "ijwkga" }]
];
const Volume2 = createLucideIcon("volume-2", __iconNode);
const EARTH_RADIUS_M = 6371e3;
function toRad(deg) {
  return deg * Math.PI / 180;
}
function calculateDistance(a, b) {
  const dLat = toRad(b.lat - a.lat);
  const dLon = toRad(b.lon - a.lon);
  const sinDLat = Math.sin(dLat / 2);
  const sinDLon = Math.sin(dLon / 2);
  const h = sinDLat * sinDLat + Math.cos(toRad(a.lat)) * Math.cos(toRad(b.lat)) * sinDLon * sinDLon;
  return 2 * EARTH_RADIUS_M * Math.asin(Math.sqrt(h));
}
function calculateHeading(from, to) {
  const dLon = toRad(to.lon - from.lon);
  const lat1 = toRad(from.lat);
  const lat2 = toRad(to.lat);
  const y = Math.sin(dLon) * Math.cos(lat2);
  const x = Math.cos(lat1) * Math.sin(lat2) - Math.sin(lat1) * Math.cos(lat2) * Math.cos(dLon);
  const bearing = (Math.atan2(y, x) * 180 / Math.PI + 360) % 360;
  if (bearing < 22.5 || bearing >= 337.5) return "north";
  if (bearing < 67.5) return "northeast";
  if (bearing < 112.5) return "east";
  if (bearing < 157.5) return "southeast";
  if (bearing < 202.5) return "south";
  if (bearing < 247.5) return "southwest";
  if (bearing < 292.5) return "west";
  return "northwest";
}
function isNearIntersection(current, previous) {
  if (previous.length < 3) return false;
  const windowSize = Math.min(previous.length, 6);
  const window = previous.slice(-windowSize);
  const totalTravel = window.reduce((sum, pt, i) => {
    if (i === 0) return sum;
    return sum + calculateDistance(window[i - 1], pt);
  }, 0);
  if (totalTravel < 30) return false;
  const lastPt = previous[previous.length - 1];
  const distFromLast = calculateDistance(lastPt, current);
  return distFromLast >= 5;
}
function totalTrailDistance(trail) {
  return trail.reduce((sum, pt, i) => {
    if (i === 0) return sum;
    return sum + calculateDistance(trail[i - 1], pt);
  }, 0);
}
function useGps() {
  const [coords, setCoords] = reactExports.useState(null);
  const [heading, setHeading] = reactExports.useState("north");
  const [isTracking, setIsTracking] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const [gpsTrail, setGpsTrail] = reactExports.useState([]);
  const watchIdRef = reactExports.useRef(null);
  const prevCoordsRef = reactExports.useRef(null);
  const startTracking = reactExports.useCallback(() => {
    if (!navigator.geolocation) {
      setError("Geolocation is not supported by your browser.");
      return;
    }
    setError(null);
    setIsTracking(true);
    watchIdRef.current = navigator.geolocation.watchPosition(
      (position) => {
        const newCoord = {
          lat: position.coords.latitude,
          lon: position.coords.longitude
        };
        setCoords(newCoord);
        setGpsTrail((prev) => [...prev, newCoord]);
        if (prevCoordsRef.current) {
          const newHeading = calculateHeading(prevCoordsRef.current, newCoord);
          setHeading(newHeading);
        }
        prevCoordsRef.current = newCoord;
      },
      (err) => {
        switch (err.code) {
          case err.PERMISSION_DENIED:
            setError(
              "Location access denied. Please enable GPS permissions in your device settings to use RunQuest."
            );
            break;
          case err.POSITION_UNAVAILABLE:
            setError(
              "Location unavailable. Make sure you're outdoors with GPS signal."
            );
            break;
          case err.TIMEOUT:
            setError("Location request timed out. Please try again.");
            break;
          default:
            setError("An unknown location error occurred.");
        }
        setIsTracking(false);
      },
      {
        enableHighAccuracy: true,
        maximumAge: 2e3,
        timeout: 1e4
      }
    );
  }, []);
  const stopTracking = reactExports.useCallback(() => {
    if (watchIdRef.current !== null) {
      navigator.geolocation.clearWatch(watchIdRef.current);
      watchIdRef.current = null;
    }
    setIsTracking(false);
  }, []);
  return {
    coords,
    heading,
    isTracking,
    error,
    startTracking,
    stopTracking,
    gpsTrail
  };
}
const INITIAL_STATE = {
  runStatus: "idle",
  genre: null,
  chapters: [],
  choices: [],
  gpsTrail: [],
  distance: 0,
  elapsedSeconds: 0,
  startedAt: null,
  endedAt: null
};
function useRun() {
  const [state, setState] = reactExports.useState(INITIAL_STATE);
  const timerRef = reactExports.useRef(null);
  const pausedAtRef = reactExports.useRef(0);
  reactExports.useEffect(() => {
    return () => {
      if (timerRef.current) clearInterval(timerRef.current);
    };
  }, []);
  const startTimer = reactExports.useCallback(() => {
    if (timerRef.current) clearInterval(timerRef.current);
    timerRef.current = setInterval(() => {
      setState((prev) => ({
        ...prev,
        elapsedSeconds: prev.elapsedSeconds + 1
      }));
    }, 1e3);
  }, []);
  const stopTimer = reactExports.useCallback(() => {
    if (timerRef.current) {
      clearInterval(timerRef.current);
      timerRef.current = null;
    }
  }, []);
  const startRun = reactExports.useCallback(
    (genre) => {
      setState({
        ...INITIAL_STATE,
        runStatus: "active",
        genre,
        startedAt: Date.now()
      });
      startTimer();
    },
    [startTimer]
  );
  const pauseRun = reactExports.useCallback(() => {
    pausedAtRef.current = Date.now();
    stopTimer();
    setState((prev) => ({ ...prev, runStatus: "paused" }));
  }, [stopTimer]);
  const resumeRun = reactExports.useCallback(() => {
    startTimer();
    setState((prev) => ({ ...prev, runStatus: "active" }));
  }, [startTimer]);
  const endRun = reactExports.useCallback(() => {
    stopTimer();
    setState((prev) => ({ ...prev, runStatus: "ended", endedAt: Date.now() }));
  }, [stopTimer]);
  const addChapter = reactExports.useCallback((chapter) => {
    setState((prev) => ({ ...prev, chapters: [...prev.chapters, chapter] }));
  }, []);
  const recordChoice = reactExports.useCallback((choice) => {
    setState((prev) => ({ ...prev, choices: [...prev.choices, choice] }));
  }, []);
  const appendGpsPoint = reactExports.useCallback((coord) => {
    setState((prev) => {
      const newTrail = [...prev.gpsTrail, coord];
      return {
        ...prev,
        gpsTrail: newTrail,
        distance: totalTrailDistance(newTrail)
      };
    });
  }, []);
  const resetRun = reactExports.useCallback(() => {
    stopTimer();
    setState(INITIAL_STATE);
  }, [stopTimer]);
  return {
    ...state,
    startRun,
    pauseRun,
    resumeRun,
    endRun,
    addChapter,
    recordChoice,
    appendGpsPoint,
    resetRun
  };
}
const MISTRAL_API_KEY = "nrayQwviJ8l6nyTdq5mTweYgrgnrz6DZ";
const CHAT_URL = "https://api.mistral.ai/v1/chat/completions";
const TTS_URL = "https://api.mistral.ai/v1/audio/speech";
const GENRE_PROMPTS = {
  Fantasy: "high fantasy with magic, dragons, ancient ruins, mystical creatures, and epic quests",
  SciFi: "science fiction with alien contact, advanced technology, space exploration references, and cyberpunk undertones",
  Horror: "atmospheric horror with creeping dread, shadowy monsters, unsettling omens, and psychological tension",
  Mystery: "noir mystery with hidden clues, suspicious characters, secret organizations, and detective intrigue",
  Romance: "romantic adventure with chance encounters, emotional tension, heartfelt moments, and passionate decisions"
};
async function generateChapter(params) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i;
  const {
    streetName1,
    streetName2,
    currentStreet,
    direction,
    genre,
    storyHistory,
    chapterNumber
  } = params;
  const historyContext = storyHistory.length > 0 ? `

STORY SO FAR:
${storyHistory.slice(-3).join("\n\n---\n\n")}` : "";
  const systemPrompt = `You are a master storyteller creating a ${GENRE_PROMPTS[genre]} story for a runner.
The story is told in second person ("you"). Every chapter must be exactly 150-200 words, immersive, and visceral.

CRITICAL IMMERSION RULE: The character is physically running THROUGH the current street right now.
Describe the street's sensory environment in vivid detail — the look and feel of "${currentStreet}", 
what surrounds it (buildings, trees, pavement, atmosphere), the ${direction}ward momentum of running.
Make the reader FEEL they are on this exact street. Weave in the street name naturally, not just as a label.
Then build to a cliffhanger at the next intersection where two paths diverge.

RESPONSE FORMAT (use this EXACT JSON structure, no other text):
{
  "chapterText": "<the full chapter text, 150-200 words, vividly describing ${currentStreet} heading ${direction}>",
  "choice1": {
    "streetName": "${streetName1}",
    "direction": "left",
    "choiceSummary": "<brief 1-sentence narrative consequence of turning onto ${streetName1}>"
  },
  "choice2": {
    "streetName": "${streetName2}",
    "direction": "right",
    "choiceSummary": "<brief 1-sentence narrative consequence of turning onto ${streetName2}>"
  }
}`;
  const userPrompt = `Chapter ${chapterNumber}: You are running ${direction} along ${currentStreet}.
Ahead, the road meets an intersection with ${streetName1} on one side and ${streetName2} on the other.
Genre: ${genre}. Vividly describe running along ${currentStreet} — its atmosphere, the feeling underfoot, what you see — then end at the intersection with a cliffhanger. 150-200 words.${historyContext}`;
  const response = await fetch(CHAT_URL, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${MISTRAL_API_KEY}`
    },
    body: JSON.stringify({
      model: "mistral-large-latest",
      messages: [
        { role: "system", content: systemPrompt },
        { role: "user", content: userPrompt }
      ],
      response_format: { type: "json_object" },
      temperature: 0.85
    })
  });
  if (!response.ok) {
    const err = await response.text();
    throw new Error(`Mistral API error: ${response.status} ${err}`);
  }
  const data = await response.json();
  const raw = ((_c = (_b = (_a = data.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) ?? "{}";
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    parsed = {
      chapterText: raw,
      choice1: {
        streetName: streetName1,
        direction: "left",
        choiceSummary: `Head left onto ${streetName1}.`
      },
      choice2: {
        streetName: streetName2,
        direction: "right",
        choiceSummary: `Turn right onto ${streetName2}.`
      }
    };
  }
  const chapterText = parsed.chapterText ?? raw;
  const choice1 = {
    streetName: ((_d = parsed.choice1) == null ? void 0 : _d.streetName) ?? streetName1,
    direction: ((_e = parsed.choice1) == null ? void 0 : _e.direction) ?? "left",
    choiceSummary: ((_f = parsed.choice1) == null ? void 0 : _f.choiceSummary) ?? `Continue onto ${streetName1}`
  };
  const choice2 = {
    streetName: ((_g = parsed.choice2) == null ? void 0 : _g.streetName) ?? streetName2,
    direction: ((_h = parsed.choice2) == null ? void 0 : _h.direction) ?? "right",
    choiceSummary: ((_i = parsed.choice2) == null ? void 0 : _i.choiceSummary) ?? `Turn onto ${streetName2}`
  };
  return { chapterText, choice1, choice2 };
}
async function generateTTS(text) {
  const trimmed = text.length > 4e3 ? `${text.slice(0, 4e3)}...` : text;
  const response = await fetch(TTS_URL, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${MISTRAL_API_KEY}`
    },
    body: JSON.stringify({
      model: "voxtral-mini-tts-2603",
      voice: "en_paul_neutral",
      input: trimmed
    })
  });
  if (!response.ok) {
    const errText = await response.text();
    throw new Error(`TTS API error: ${response.status} ${errText}`);
  }
  const arrayBuffer = await response.arrayBuffer();
  console.log(
    `[TTS] arrayBuffer received — size: ${arrayBuffer.byteLength} bytes`
  );
  if (arrayBuffer.byteLength === 0) {
    throw new Error("TTS returned empty audio buffer");
  }
  return arrayBuffer;
}
const DEBOUNCE_MS = 1e3;
let lastCallTime = 0;
async function throttledFetch(url) {
  const now = Date.now();
  const elapsed = now - lastCallTime;
  if (elapsed < DEBOUNCE_MS) {
    await new Promise(
      (resolve) => setTimeout(resolve, DEBOUNCE_MS - elapsed)
    );
  }
  lastCallTime = Date.now();
  return fetch(url, { headers: { "Accept-Language": "en" } });
}
function extractRoadName(addr) {
  if (!addr) return "";
  return addr.road ?? addr.pedestrian ?? addr.footway ?? addr.path ?? addr.cycleway ?? addr.residential ?? "";
}
async function findIntersectingStreets(coord, currentStreet, radiusMeters = 50) {
  var _a;
  const query = `
[out:json][timeout:10];
(
  way(around:${radiusMeters},${coord.lat},${coord.lon})[highway][name];
);
out body;
>;
out skel qt;`;
  const overpassUrl = `https://overpass-api.de/api/interpreter?data=${encodeURIComponent(query)}`;
  const seen = /* @__PURE__ */ new Set();
  const intersecting = [];
  const currentLower = currentStreet.toLowerCase();
  if (currentStreet) seen.add(currentLower);
  try {
    const res = await fetch(overpassUrl, { signal: AbortSignal.timeout(8e3) });
    if (!res.ok) return intersecting;
    const data = await res.json();
    const wayNames = [];
    for (const el of data.elements) {
      if (el.type === "way" && ((_a = el.tags) == null ? void 0 : _a.name)) {
        const name = el.tags.name;
        if (!seen.has(name.toLowerCase())) {
          wayNames.push(name);
          seen.add(name.toLowerCase());
        }
      }
    }
    for (const name of wayNames) {
      if (currentStreet && name.toLowerCase().includes(currentLower)) continue;
      intersecting.push(name);
    }
  } catch {
  }
  return intersecting;
}
async function reverseGeocode(coord) {
  const reverseUrl = `https://nominatim.openstreetmap.org/reverse?lat=${coord.lat}&lon=${coord.lon}&format=json&addressdetails=1`;
  let primaryRoad = "";
  try {
    const res = await throttledFetch(reverseUrl);
    if (res.ok) {
      const data = await res.json();
      if (!data.error) {
        primaryRoad = extractRoadName(data.address);
      }
    }
  } catch {
    return { street: "", nearbyStreets: [] };
  }
  if (primaryRoad) {
    const intersecting = await findIntersectingStreets(coord, primaryRoad, 45);
    if (intersecting.length > 0) {
      return { street: primaryRoad, nearbyStreets: intersecting };
    }
  }
  const delta = 4e-4;
  const minLon = coord.lon - delta;
  const minLat = coord.lat - delta;
  const maxLon = coord.lon + delta;
  const maxLat = coord.lat + delta;
  const searchUrl = `https://nominatim.openstreetmap.org/search?q=&format=json&addressdetails=1&limit=10&bounded=1&viewbox=${minLon},${minLat},${maxLon},${maxLat}`;
  const seen = /* @__PURE__ */ new Set();
  const nearbyStreets = [];
  if (primaryRoad) {
    seen.add(primaryRoad.toLowerCase());
  }
  try {
    const searchRes = await throttledFetch(searchUrl);
    if (searchRes.ok) {
      const results = await searchRes.json();
      for (const item of results) {
        if (item.class !== "highway" && item.type !== "road") {
          const roadFromAddr = extractRoadName(item.address);
          if (roadFromAddr && roadFromAddr.length > 2 && !seen.has(roadFromAddr.toLowerCase())) {
            seen.add(roadFromAddr.toLowerCase());
            nearbyStreets.push(roadFromAddr);
          }
          continue;
        }
        const roadName = extractRoadName(item.address);
        if (roadName && roadName.length > 2 && !seen.has(roadName.toLowerCase())) {
          seen.add(roadName.toLowerCase());
          nearbyStreets.push(roadName);
        }
      }
    }
  } catch {
  }
  return { street: primaryRoad, nearbyStreets };
}
const GENRES = ["Fantasy", "SciFi", "Horror", "Mystery", "Romance"];
const GENRE_ICONS = {
  Fantasy: "🐉",
  SciFi: "🚀",
  Horror: "💀",
  Mystery: "🔍",
  Romance: "💫"
};
const GENRE_DESC = {
  Fantasy: "Dragons, magic & ancient ruins",
  SciFi: "Aliens, tech & cyberpunk worlds",
  Horror: "Shadows, monsters & dread",
  Mystery: "Clues, secrets & noir intrigue",
  Romance: "Chance encounters & passion"
};
function GenreSelector({ onStart }) {
  const [selected, setSelected] = reactExports.useState(null);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "flex-1 flex flex-col items-center justify-center px-4 py-10 bg-background",
      "data-ocid": "genre-select-screen",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-md w-full flex flex-col gap-6", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mx-auto mb-4 w-16 h-16 rounded-2xl bg-accent/15 border border-accent/30 flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Swords, { size: 32, className: "text-accent" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "font-display font-bold text-3xl text-foreground tracking-tight", children: "New Adventure" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground mt-2 text-sm max-w-xs mx-auto", children: "Choose your story genre. Your tale begins at the first real intersection you cross." })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "grid grid-cols-2 gap-3 sm:grid-cols-3",
            "data-ocid": "genre-selector",
            children: GENRES.map((genre) => {
              const isSelected = selected === genre;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  type: "button",
                  "data-ocid": `genre-btn-${genre.toLowerCase()}`,
                  onClick: () => setSelected(genre),
                  className: `rounded-xl border p-4 flex flex-col items-center gap-2 transition-smooth min-h-[110px] text-left ${isSelected ? `${GENRE_BG_COLORS[genre]} scale-105 ring-2 ring-offset-2 ring-offset-background` : "bg-card border-border/40 hover:bg-muted/50"}`,
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-3xl", children: GENRE_ICONS[genre] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "span",
                      {
                        className: `font-display font-bold text-sm ${isSelected ? GENRE_COLORS[genre] : "text-foreground"}`,
                        children: genre
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "span",
                      {
                        className: `text-xs text-center leading-tight ${isSelected ? `${GENRE_COLORS[genre]} opacity-80` : "text-muted-foreground"}`,
                        children: GENRE_DESC[genre]
                      }
                    ),
                    isSelected && /* @__PURE__ */ jsxRuntimeExports.jsx(
                      Badge,
                      {
                        variant: "secondary",
                        className: `text-xs mt-auto ${GENRE_BG_COLORS[genre]} ${GENRE_COLORS[genre]} border`,
                        children: "Selected"
                      }
                    )
                  ]
                },
                genre
              );
            })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            size: "lg",
            disabled: !selected,
            onClick: () => selected && onStart(selected),
            "data-ocid": "start-run-btn",
            className: `w-full min-h-[56px] text-base font-bold gap-2 transition-smooth ${selected ? GENRE_BUTTON_COLORS[selected] : ""}`,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { size: 20 }),
              selected ? `Start ${selected} Adventure` : "Select a Genre First"
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground text-center", children: "GPS access required — enable location before starting." })
      ] })
    }
  );
}
delete L.Icon.Default.prototype._getIconUrl;
L.Icon.Default.mergeOptions({
  iconRetinaUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png",
  iconUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png",
  shadowUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png"
});
function createPositionIcon() {
  return L.divIcon({
    className: "",
    html: `<div style="
      width:18px;height:18px;border-radius:50%;
      background:oklch(0.72 0.24 200);
      border:3px solid white;
      box-shadow:0 0 12px oklch(0.72 0.24 200 / 0.8);
    "></div>`,
    iconSize: [18, 18],
    iconAnchor: [9, 9]
  });
}
function createChapterIcon(num) {
  return L.divIcon({
    className: "",
    html: `<div style="
      width:24px;height:24px;border-radius:50%;
      background:oklch(0.75 0.22 60);
      border:2px solid white;
      display:flex;align-items:center;justify-content:center;
      font-size:10px;font-weight:bold;color:#000;
      box-shadow:0 0 8px oklch(0.75 0.22 60 / 0.6);
    ">${num}</div>`,
    iconSize: [24, 24],
    iconAnchor: [12, 12]
  });
}
function RunMap({ coords, trail, chapterPoints }) {
  const containerRef = reactExports.useRef(null);
  const mapRef = reactExports.useRef(null);
  const posMarkerRef = reactExports.useRef(null);
  const trailPolylineRef = reactExports.useRef(null);
  const chapterMarkersRef = reactExports.useRef([]);
  reactExports.useEffect(() => {
    if (!containerRef.current || mapRef.current) return;
    const map = L.map(containerRef.current, {
      center: [51.505, -0.09],
      zoom: 17,
      zoomControl: false,
      attributionControl: false
    });
    L.tileLayer(
      "https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png",
      {
        maxZoom: 19,
        subdomains: "abcd"
      }
    ).addTo(map);
    L.control.attribution({ prefix: "© OSM · CartoDB" }).addTo(map);
    mapRef.current = map;
    return () => {
      map.remove();
      mapRef.current = null;
    };
  }, []);
  reactExports.useEffect(() => {
    if (!mapRef.current || !coords) return;
    const latlng = [coords.lat, coords.lon];
    if (!posMarkerRef.current) {
      posMarkerRef.current = L.marker(latlng, {
        icon: createPositionIcon()
      }).addTo(mapRef.current);
    } else {
      posMarkerRef.current.setLatLng(latlng);
    }
    mapRef.current.setView(latlng, mapRef.current.getZoom(), { animate: true });
  }, [coords]);
  reactExports.useEffect(() => {
    if (!mapRef.current || trail.length < 2) return;
    const points = trail.map((c) => [c.lat, c.lon]);
    if (!trailPolylineRef.current) {
      trailPolylineRef.current = L.polyline(points, {
        color: "oklch(0.75 0.22 60)",
        weight: 3,
        opacity: 0.85
      }).addTo(mapRef.current);
    } else {
      trailPolylineRef.current.setLatLngs(points);
    }
  }, [trail]);
  reactExports.useEffect(() => {
    if (!mapRef.current) return;
    const existingCount = chapterMarkersRef.current.length;
    for (let i = existingCount; i < chapterPoints.length; i++) {
      const pt = chapterPoints[i];
      if (!pt || pt.lat === 0 && pt.lon === 0) continue;
      const marker = L.marker([pt.lat, pt.lon], {
        icon: createChapterIcon(i + 1)
      }).addTo(mapRef.current);
      chapterMarkersRef.current.push(marker);
    }
  }, [chapterPoints]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      ref: containerRef,
      className: "w-full h-full",
      "aria-label": "Run map",
      role: "img"
    }
  );
}
function generateTitle(genre, chapterCount) {
  const prefixes = {
    Fantasy: [
      "The Dragon's Path",
      "Quest of the Ancient Roads",
      "The Runecrossing"
    ],
    SciFi: ["Sector Zero Run", "Neon Circuit Marathon", "The Hyperline"],
    Horror: ["The Midnight Crossing", "Shadows on the Trail", "The Dark Mile"],
    Mystery: ["The Street Cipher", "Trail of Shadows", "The Unknown Route"],
    Romance: ["Hearts in Motion", "The Running Encounter", "Paths That Cross"]
  };
  const g = genre ?? "Fantasy";
  const list = prefixes[g] ?? prefixes.Fantasy;
  const base = list[chapterCount % list.length];
  return `${base} — ${chapterCount} Chapter${chapterCount !== 1 ? "s" : ""}`;
}
function SaveRunModal({
  run,
  isSaving,
  saveSlug,
  onSave,
  onDiscard
}) {
  const [title, setTitle] = reactExports.useState(
    () => generateTitle(run.genre, run.chapters.length)
  );
  const distKm = (run.distance / 1e3).toFixed(2);
  const mins = Math.floor(run.elapsedSeconds / 60);
  const secs = run.elapsedSeconds % 60;
  const timeStr = `${String(mins).padStart(2, "0")}:${String(secs).padStart(2, "0")}`;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "flex-1 flex flex-col items-center justify-center px-4 py-10 bg-background",
      "data-ocid": "save-run-screen",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-md w-full flex flex-col gap-5", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { size: 36, className: "text-accent mx-auto mb-3" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display font-bold text-2xl text-foreground", children: "Adventure Complete!" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-muted-foreground text-sm mt-1", children: [
            run.chapters.length,
            " chapters · ",
            distKm,
            " km · ",
            timeStr
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "label",
            {
              htmlFor: "run-title",
              className: "text-sm font-semibold text-foreground",
              children: "Adventure Title"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              id: "run-title",
              type: "text",
              value: title,
              onChange: (e) => setTitle(e.target.value),
              className: "w-full rounded-lg border border-input bg-card px-4 py-3 text-foreground text-base font-display focus:outline-none focus:ring-2 focus:ring-accent/50 min-h-[44px]",
              "data-ocid": "run-title-input",
              disabled: isSaving
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-3 gap-3", children: [
          { label: "Distance", value: `${distKm} km` },
          { label: "Duration", value: timeStr },
          { label: "Chapters", value: String(run.chapters.length) }
        ].map((s) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "bg-card border border-border/40 rounded-lg p-3 text-center",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground uppercase tracking-wider mb-0.5", children: s.label }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-display font-bold text-lg text-foreground", children: s.value })
            ]
          },
          s.label
        )) }),
        isSaving ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-12 w-full" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-12 w-full" })
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              size: "lg",
              onClick: () => onSave(title),
              disabled: !title.trim(),
              "data-ocid": "save-run-btn",
              className: "min-h-[52px] font-bold gap-2 bg-accent text-accent-foreground hover:bg-accent/90",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Save, { size: 18 }),
                "Save & View Story"
              ]
            }
          ),
          saveSlug && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              variant: "outline",
              size: "lg",
              "data-ocid": "view-story-btn",
              className: "min-h-[52px] gap-2",
              onClick: () => onDiscard(),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { size: 18 }),
                "View Shared Story"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              variant: "ghost",
              size: "lg",
              onClick: onDiscard,
              "data-ocid": "discard-run-btn",
              className: "min-h-[52px] gap-2 text-muted-foreground hover:text-destructive",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { size: 18 }),
                "Discard Run"
              ]
            }
          )
        ] })
      ] })
    }
  );
}
function ActiveRun() {
  const navigate = useNavigate();
  const { genre: urlGenre } = useSearch({ from: "/run" });
  const [phase, setPhase] = reactExports.useState(
    urlGenre ? "running" : "genre-select"
  );
  const [chapterResult, setChapterResult] = reactExports.useState(
    null
  );
  const [isGenerating, setIsGenerating] = reactExports.useState(false);
  const [isPlaying, setIsPlaying] = reactExports.useState(false);
  const [storyHistory, setStoryHistory] = reactExports.useState([]);
  const [geocodeError, setGeocodeError] = reactExports.useState(null);
  const [saveSlug, setSaveSlug] = reactExports.useState(null);
  const audioContextRef = reactExports.useRef(null);
  const currentSourceRef = reactExports.useRef(null);
  const skipCallbackRef = reactExports.useRef(null);
  const intersectionCheckRef = reactExports.useRef(
    null
  );
  const lastIntersectionRef = reactExports.useRef("");
  const chapterTextRef = reactExports.useRef(null);
  const hasGeneratedOpeningChapterRef = reactExports.useRef(false);
  const currentStreetRef = reactExports.useRef("");
  const run = useRun();
  const gps = useGps();
  const { saveRun, isSaving } = useBackend();
  reactExports.useEffect(() => {
    return () => {
      var _a, _b;
      (_a = currentSourceRef.current) == null ? void 0 : _a.stop();
      (_b = audioContextRef.current) == null ? void 0 : _b.close();
      audioContextRef.current = null;
    };
  }, []);
  const stopNarration = reactExports.useCallback(() => {
    var _a;
    try {
      (_a = currentSourceRef.current) == null ? void 0 : _a.stop();
    } catch {
    }
    currentSourceRef.current = null;
    if (skipCallbackRef.current) {
      skipCallbackRef.current();
      skipCallbackRef.current = null;
    }
    setIsPlaying(false);
  }, []);
  const handleSkipNarration = reactExports.useCallback(() => {
    stopNarration();
  }, [stopNarration]);
  const playTTS = reactExports.useCallback(async (text) => {
    var _a;
    setIsPlaying(true);
    try {
      console.log("[TTS] Fetching audio buffer…");
      const arrayBuffer = await generateTTS(text);
      if (!audioContextRef.current || audioContextRef.current.state === "closed") {
        console.log("[TTS] Creating new AudioContext");
        audioContextRef.current = new AudioContext();
      }
      const ctx = audioContextRef.current;
      if (ctx.state === "suspended") {
        console.log("[TTS] Resuming suspended AudioContext");
        await ctx.resume();
      }
      console.log("[TTS] AudioContext state:", ctx.state);
      console.log("[TTS] Decoding audio data…");
      const audioBuffer = await ctx.decodeAudioData(arrayBuffer);
      console.log(
        "[TTS] Audio decoded — duration:",
        audioBuffer.duration.toFixed(1),
        "s"
      );
      try {
        (_a = currentSourceRef.current) == null ? void 0 : _a.stop();
      } catch {
      }
      const source = ctx.createBufferSource();
      source.buffer = audioBuffer;
      source.connect(ctx.destination);
      currentSourceRef.current = source;
      await new Promise((resolve) => {
        skipCallbackRef.current = () => {
          console.log("[TTS] playback skipped by user");
          resolve();
        };
        source.onended = () => {
          console.log("[TTS] onended fired — narration complete");
          skipCallbackRef.current = null;
          currentSourceRef.current = null;
          setIsPlaying(false);
          resolve();
        };
        console.log("[TTS] Calling source.start()…");
        source.start(0);
      });
    } catch (err) {
      console.error("[TTS] playback error:", err);
      setIsPlaying(false);
    }
  }, []);
  const autoStartedRef = reactExports.useRef(false);
  const startRun = run.startRun;
  reactExports.useEffect(() => {
    if (autoStartedRef.current) return;
    if (!urlGenre) return;
    autoStartedRef.current = true;
    const validGenres = [
      "Fantasy",
      "SciFi",
      "Horror",
      "Mystery",
      "Romance"
    ];
    const genre2 = validGenres.includes(urlGenre) ? urlGenre : "Fantasy";
    startRun(genre2);
  }, [urlGenre, startRun]);
  const triggerChapter = reactExports.useCallback(
    async (street1, street2, onStreet) => {
      if (!run.genre) return;
      const activeStreet = onStreet ?? currentStreetRef.current ?? street1;
      setIsGenerating(true);
      setChapterResult(null);
      const chapterNumber = run.chapters.length + 1;
      try {
        const result = await generateChapter({
          streetName1: street1,
          streetName2: street2,
          currentStreet: activeStreet,
          direction: gps.heading,
          genre: run.genre,
          storyHistory,
          chapterNumber
        });
        setChapterResult(result);
        setStoryHistory((prev) => [...prev, result.chapterText]);
        run.addChapter({
          text: result.chapterText,
          streetName1: street1,
          streetName2: street2,
          direction: gps.heading,
          choiceIndex: -1
        });
        setIsGenerating(false);
        setTimeout(() => {
          var _a;
          (_a = chapterTextRef.current) == null ? void 0 : _a.scrollIntoView({
            behavior: "smooth",
            block: "start"
          });
        }, 200);
        await playTTS(result.chapterText);
      } catch (err) {
        setIsGenerating(false);
        setGeocodeError(
          err instanceof Error ? err.message : "Story generation failed."
        );
      }
    },
    [run, gps.heading, storyHistory, playTTS]
  );
  const startTracking = gps.startTracking;
  const stopTracking = gps.stopTracking;
  reactExports.useEffect(() => {
    if (phase !== "running") return;
    startTracking();
    return () => {
      stopTracking();
      if (intersectionCheckRef.current)
        clearInterval(intersectionCheckRef.current);
    };
  }, [phase, startTracking, stopTracking]);
  const appendGpsPoint = run.appendGpsPoint;
  const runStatus = run.runStatus;
  reactExports.useEffect(() => {
    if (gps.coords && phase === "running" && runStatus === "active") {
      appendGpsPoint(gps.coords);
    }
  }, [gps.coords, phase, runStatus, appendGpsPoint]);
  reactExports.useEffect(() => {
    if (phase !== "running") return;
    if (!gps.coords) return;
    if (hasGeneratedOpeningChapterRef.current) return;
    if (isGenerating || isPlaying) return;
    hasGeneratedOpeningChapterRef.current = true;
    (async () => {
      try {
        const geo = await reverseGeocode(gps.coords);
        const allStreets = [geo.street, ...geo.nearbyStreets].filter(Boolean);
        const street1 = allStreets[0] ?? "the starting line";
        const street2 = allStreets[1] ?? "the open road";
        currentStreetRef.current = street1;
        lastIntersectionRef.current = `${street1}|${street2}`;
        await triggerChapter(street1, street2, street1);
      } catch {
        hasGeneratedOpeningChapterRef.current = false;
      }
    })();
  }, [phase, gps.coords, isGenerating, isPlaying, triggerChapter]);
  const gpsTrail = gps.gpsTrail;
  reactExports.useEffect(() => {
    if (phase !== "running") return;
    intersectionCheckRef.current = setInterval(async () => {
      if (!gps.coords || runStatus !== "active" || isGenerating || isPlaying)
        return;
      if (!isNearIntersection(gps.coords, gpsTrail)) return;
      const geo = await reverseGeocode(gps.coords);
      const allStreets = [geo.street, ...geo.nearbyStreets].filter(Boolean);
      if (allStreets.length < 2) return;
      const key = `${allStreets[0]}|${allStreets[1]}`;
      if (key === lastIntersectionRef.current) return;
      lastIntersectionRef.current = key;
      currentStreetRef.current = allStreets[0];
      await triggerChapter(allStreets[0], allStreets[1], allStreets[0]);
    }, 7e3);
    return () => {
      if (intersectionCheckRef.current)
        clearInterval(intersectionCheckRef.current);
    };
  }, [
    phase,
    gps.coords,
    runStatus,
    isGenerating,
    isPlaying,
    gpsTrail,
    triggerChapter
  ]);
  const handleStartRun = (genre2) => {
    if (!audioContextRef.current || audioContextRef.current.state === "closed") {
      audioContextRef.current = new AudioContext();
      console.log("[TTS] AudioContext created on user gesture");
    }
    if (audioContextRef.current.state === "suspended") {
      audioContextRef.current.resume().then(() => {
        console.log("[TTS] AudioContext resumed on user gesture");
      });
    }
    run.startRun(genre2);
    setPhase("running");
  };
  const handleEndRun = () => {
    run.endRun();
    gps.stopTracking();
    stopNarration();
    setPhase("save");
  };
  const handleSaveRun = async (title) => {
    if (!run.genre) return;
    const slug = await saveRun({
      title,
      genre: run.genre,
      chapters: run.chapters,
      choices: run.choices.map((c) => c.choiceSummary),
      gpsRoute: run.gpsTrail,
      startTime: run.startedAt ?? Date.now(),
      endTime: run.endedAt ?? Date.now(),
      totalDistance: run.distance
    });
    setSaveSlug(slug);
    navigate({ to: "/story/$slug", params: { slug } });
  };
  if (phase === "genre-select") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(GenreSelector, { onStart: handleStartRun });
  }
  if (phase === "save") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SaveRunModal,
      {
        run,
        isSaving,
        saveSlug,
        onSave: handleSaveRun,
        onDiscard: () => navigate({ to: "/" })
      }
    );
  }
  const genre = run.genre ?? "Fantasy";
  const genreColor = GENRE_COLORS[genre];
  const genreBg = GENRE_BG_COLORS[genre];
  const genreGlow = GENRE_GLOW[genre];
  const mins = Math.floor(run.elapsedSeconds / 60);
  const secs = run.elapsedSeconds % 60;
  const timeStr = `${String(mins).padStart(2, "0")}:${String(secs).padStart(2, "0")}`;
  const distKm = (run.distance / 1e3).toFixed(2);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 flex flex-col bg-background min-h-0 overflow-hidden", children: [
    gps.error && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex items-start gap-3 p-4 bg-destructive/10 border-b border-destructive/30 text-sm",
        "data-ocid": "gps-error-banner",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            TriangleAlert,
            {
              size: 18,
              className: "text-destructive mt-0.5 flex-shrink-0"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-semibold text-destructive", children: "GPS Unavailable" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground mt-0.5", children: gps.error }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-muted-foreground mt-1", children: [
              "To enable:",
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Settings → Privacy → Location Services → RunQuest → While Using" })
            ] })
          ] })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "h-[40vh] min-h-[220px] w-full relative",
        "data-ocid": "run-map",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            RunMap,
            {
              coords: gps.coords,
              trail: gps.gpsTrail,
              chapterPoints: run.chapters.map((_ch, i) => {
                var _a, _b;
                return {
                  lat: run.gpsTrail[i * 10] ? run.gpsTrail[i * 10].lat : ((_a = run.gpsTrail[run.gpsTrail.length - 1]) == null ? void 0 : _a.lat) ?? 0,
                  lon: run.gpsTrail[i * 10] ? run.gpsTrail[i * 10].lon : ((_b = run.gpsTrail[run.gpsTrail.length - 1]) == null ? void 0 : _b.lon) ?? 0
                };
              })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-3 left-3 z-[500]", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Badge,
            {
              className: `font-display font-bold tracking-widest text-xs px-2.5 py-1 border ${genreBg} ${genreColor} uppercase`,
              "data-ocid": "genre-badge",
              children: [
                genre,
                " RUN"
              ]
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "absolute bottom-0 left-0 right-0 z-[500] bg-background/80 backdrop-blur-sm border-t border-border/40 px-4 py-2 flex items-center justify-between",
              "data-ocid": "run-stats",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(StatCell, { label: "DIST", value: `${distKm} km` }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(StatCell, { label: "TIME", value: timeStr }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(StatCell, { label: "CHAPTERS", value: String(run.chapters.length) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    Button,
                    {
                      variant: "outline",
                      size: "sm",
                      onClick: run.runStatus === "paused" ? run.resumeRun : run.pauseRun,
                      className: "min-h-[44px] min-w-[44px] px-3 border-border/60 text-xs gap-1.5",
                      "data-ocid": "pause-resume-btn",
                      children: [
                        run.runStatus === "paused" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { size: 14 }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Pause, { size: 14 }),
                        run.runStatus === "paused" ? "Resume" : "Pause"
                      ]
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    Button,
                    {
                      variant: "destructive",
                      size: "sm",
                      onClick: handleEndRun,
                      className: "min-h-[44px] min-w-[44px] px-3 text-xs gap-1.5",
                      "data-ocid": "end-run-btn",
                      children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(Square, { size: 14 }),
                        "End"
                      ]
                    }
                  )
                ] })
              ]
            }
          )
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 flex flex-col overflow-y-auto overscroll-contain pb-4", children: [
      isGenerating && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "flex flex-col items-center gap-4 p-8 text-center",
          "data-ocid": "chapter-loading",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "div",
              {
                className: `w-16 h-16 rounded-full border-4 border-t-transparent ${genreColor.replace("text-", "border-")} animate-spin`,
                role: "status",
                "aria-label": "Generating chapter"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: `font-display font-bold text-lg ${genreColor}`, children: "The Story Unfolds…" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm mt-1", children: "Weaving your adventure through these streets" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full max-w-sm space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-full" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-5/6" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-4/6" })
            ] })
          ]
        }
      ),
      !isGenerating && chapterResult && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          ref: chapterTextRef,
          className: `mx-4 mt-4 rounded-xl border p-4 ${genreBg} ${genreGlow} transition-smooth`,
          "data-ocid": "chapter-panel",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-3", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: `font-display font-bold text-base ${genreColor}`, children: [
                "Chapter ",
                run.chapters.length
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { variant: "secondary", className: "text-xs", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(MapPin, { size: 10, className: "mr-1" }),
                chapterResult.choice1.streetName,
                " &",
                " ",
                chapterResult.choice2.streetName
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground text-base leading-relaxed whitespace-pre-wrap break-words", children: chapterResult.chapterText })
          ]
        }
      ),
      !isGenerating && !chapterResult && !gps.error && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "flex flex-col items-center gap-3 p-8 text-center",
          "data-ocid": "waiting-state",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Swords, { size: 36, className: `${genreColor} opacity-60` }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: `font-display font-bold text-lg ${genreColor}`, children: "Your Adventure Awaits" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm max-w-xs", children: gps.coords ? "Summoning your opening chapter…" : "Acquiring GPS signal — your story begins the moment you're located." })
          ]
        }
      ),
      geocodeError && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mx-4 mt-3 flex items-start gap-2 p-3 bg-destructive/10 rounded-lg border border-destructive/30 text-sm", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          TriangleAlert,
          {
            size: 16,
            className: "text-destructive flex-shrink-0 mt-0.5"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: geocodeError })
      ] }),
      isPlaying && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: `mx-4 mt-4 rounded-xl border px-4 py-3 flex items-center gap-3 animate-glow-pulse ${genreBg}`,
          "data-ocid": "tts-indicator",
          "aria-live": "polite",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Volume2, { size: 20, className: `${genreColor} flex-shrink-0` }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: `font-display font-semibold text-sm ${genreColor}`, children: "Listening to your story…" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "Keep running — next chapter triggers automatically" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-0.5 shrink-0", children: [1, 2, 3, 4, 5].map((b) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "div",
              {
                className: `w-1 rounded-full ${genreColor.replace("text-", "bg-")} animate-pulse`,
                style: {
                  height: `${8 + b * 4}px`,
                  animationDelay: `${b * 0.12}s`
                }
              },
              b
            )) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                variant: "ghost",
                size: "sm",
                onClick: handleSkipNarration,
                className: "shrink-0 h-8 px-2 text-xs gap-1 text-muted-foreground hover:text-foreground",
                "aria-label": "Skip narration",
                "data-ocid": "skip-narration-btn",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { size: 14 }),
                  "Skip"
                ]
              }
            )
          ]
        }
      )
    ] })
  ] });
}
function StatCell({ label, value }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center min-w-0", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-bold text-muted-foreground uppercase tracking-widest", children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-display font-bold text-base text-foreground tabular-nums", children: value })
  ] });
}
export {
  ActiveRun as default
};
